# ══════════════════════════════════════
# 镜像版本（由 CI/CD 或 upgrade.sh 更新）
# ══════════════════════════════════════
# Hub 后端镜像仓库地址
HUB_IMAGE=ghcr.io/adambear22/nodepass-hub
# Hub 镜像版本标签（示例：v1.2.3 / latest）
HUB_VERSION=latest
# Frontend 镜像仓库地址
FRONTEND_IMAGE=ghcr.io/adambear22/nodepass-frontend
# Frontend 镜像版本标签
FRONTEND_VERSION=latest
# Agent 镜像仓库地址
AGENT_IMAGE=ghcr.io/adambear22/nodepass-agent
# Agent 镜像版本标签
AGENT_VERSION=latest

# ══════════════════════════════════════
# 域名（用于 Caddy 自动 HTTPS 和 Telegram Webhook）
# ══════════════════════════════════════
# 替换为你的域名，确保 DNS 已指向服务器
DOMAIN=hub.example.com

# ══════════════════════════════════════
# 数据库配置
# ══════════════════════════════════════
# PostgreSQL 用户名
POSTGRES_USER=nodepass
# PostgreSQL 密码（必须修改为高强度密码）
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
# PostgreSQL 数据库名
POSTGRES_DB=nodepass_hub

# ══════════════════════════════════════
# 应用配置
# ══════════════════════════════════════
# Hub 日志级别：debug/info/warn/error
LOG_LEVEL=info

# ══════════════════════════════════════
# 以下密钥通过 Docker Secrets 管理，不在此文件配置
# 见 deploy/secrets/ 目录和 setup.sh 的生成逻辑
# ══════════════════════════════════════
# jwt_private.pem            → RS256 JWT 签名私钥（setup.sh 自动生成）
# jwt_public.pem             → RS256 JWT 验证公钥（setup.sh 自动生成）
# agent_hmac_secret.txt      → Agent HMAC 认证密钥（setup.sh 自动生成）
# internal_token.txt         → 内部接口访问令牌（setup.sh / deploy.sh 自动生成）
# telegram_bot_token.txt     → Telegram Bot Token（setup.sh 交互式输入）
# external_api_key.txt       → 外部 API 访问密钥（setup.sh 自动生成）
